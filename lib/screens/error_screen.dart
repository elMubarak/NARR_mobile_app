import 'package:flutter/material.dart';

class ErrorNavigation extends StatelessWidget {
  static const String id = 'ErrorNavigation';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Error !'),
      ),
    );
  }
}
